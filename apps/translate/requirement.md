# Requirements

No dependencies or environment variables required.

## Notes

- Uses MyMemory Translation API (free, no key needed)
- Rate limit: ~5000 characters/day for anonymous usage
- For higher limits, register at mymemory.translated.net for a free API key
