# Requirements

No dependencies or environment variables required.

## Notes

- Uses the Open-Meteo API (free, no key needed, no rate limits for normal usage)
- All three actions (`find_city`, `current`, `forecast`) are plain HTTP GET — no CLI tools required
