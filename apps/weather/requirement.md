# Requirements

No dependencies or environment variables required.

## Notes

- Uses Open-Meteo API (free, no key needed, no rate limits for normal usage)
- `find_city` and `current` use HTTP GET actions (no CLI tools needed)
- `forecast` requires `curl` and `python3` (pre-installed on most systems)
